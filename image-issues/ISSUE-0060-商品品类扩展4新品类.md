---
id: ISSUE-0060
title: 商品品类扩展 B 线——首批 4 新品类（服装/美妆/鞋类/数码）注册表制 + 品类卡 + 选择器
status: 修复中        # spec 定稿派单；卡草案 coordinator 派子 agent 出稿、dev/frontend-b 代码可随卡落地；真图验收等 key
severity: P2          # 用户直接提需求（增加品类）；治「万物皆花生」、扩获客品类面；非资损非阻断
reporter: PM          # 用户 2026-07-08 提需求（coordinator #1031 转达），spec 定稿 97c4978，PM 开条
owner: 提示词+开发+frontend-b # 子agent出卡草案(prompt权威补复核)→dev(注册表+卡闸+openapi)→frontend-b(选择器)；并行 A 线
created: 2026-07-08
updated: 2026-07-08
related:
  - PRD: §3.12 套图/卡体系（CategoryCardRegistry 现 MVP 只 FOOD）、§3.18 商品品类扩展
  - spec: docs/superpowers/specs/2026-07-08-chat-knowledge-categories-design.md（coordinator 定稿 97c4978，B 线）
  - issue: ISSUE-0059（A 线 chat 改造，并行独立）、ISSUE-0053（recipe.category 已有字段=天然兼容）
  - 卡体系: image-prompt category-cards（品类卡）× 通用产品保真块（不动、品类卡是叠加层）× 卡↔code 逐字 pytest 闸
  - 记忆: project_image_type_cards（卡体系六类+逐字核对闸）、project_realtest_peanut_focus（现食品为主）
  - 群聊: image-gen#1 #1031（coordinator 派单）
---

## 定性（用户 2026-07-08 提需求，coordinator #1031 转达）
用户要「增加品类：衣服/美妆/鞋/数码等」。→ **注册表制**扩品类：首批 4 新增 + 现有 FOOD，加品类=加卡+注册、不动架构。治「万物皆花生」（现只 FOOD 保真块、非食品产品被食品语汇污染）。

## B1. 品类清单（首批 4 新增 + 现有）
`FOOD 食品`（现有）+ **`FASHION 服装` / `BEAUTY 美妆` / `SHOES 鞋类` / `DIGITAL 数码`**。"等等"后续按需增（注册表制）。

## B2. 卡体系（prompt 权威域）
- 每品类一张品类卡（`image-prompt/category-cards/`）：品类特性块——材质/质感/常见卖点/场景语汇/禁忌（如美妆肤感与质地、数码金属质感与参数化卖点、服装面料垂感与模特/平铺、鞋侧 45° 主角度）。
- **通用产品保真块（产品中性）不动**——品类卡是**叠加层**。
- **卡↔code 逐字核对 pytest 闸照旧**：卡文件与 `CategoryCardRegistry` 硬编码副本逐字同步（dev 落常量）。
- **卡草案来源**：prompt 窗口不在线 → coordinator 派子 agent 出稿 → **prompt 权威后补复核**（落地走卡闸纪律、不阻先行）。

## B3. 代码与 UI（零建表）
- **dev**：`CategoryCardRegistry` 注册 4 新品类（常量与卡逐字同步、扩 pytest 卡闸覆盖 **9→13 块**）；listing 请求 `category` 校验放开为 **5 值**；chat 工具 schema 的 category 同步；openapi 再生。**零建表**（category 本就是请求字段）。
- **frontend-b**：`/set` 工作台**品类选择器**（下拉、默认食品）+ codegen；配方展示/复用带品类（**0053 的 `recipe.category` 已有字段=天然兼容**）；chat 澄清轮品类问法随 openapi。

## B4. 验收标准（QA，⚠️ 真图部分依赖 key 恢复）
① **品类特性生效**：5 品类各真出 1 套小图（n=3）看品类特性块生效（美妆有质感/数码有产品感/服装有面料垂感/**不再「万物皆花生」**）+ **各品类真实品牌文字保真**（素材带真实品牌字）——**≈¥6，key 恢复后跑**。② 卡闸 pytest 逐字核对绿。③ 前端选择器 + chat 品类参数 + 配方回显带品类。④ **FOOD 全链零回归**。⑤ **品类精选图入成果区**（见 B5）：新品类精选图在首页成果区正常展示（图型角标/caption/配方弹卡/做同款沿 0053 不变）、showcase 泄漏扫描仍干净。
> 代码/卡可先落地，真图验收①⑤排 key 恢复后；品类可用性随真图验收公告。

## B5. 品类精选图入首页成果区（用户 #1035 追加，获客面更宽）
- **素材**：`image-qa/品类扩展素材/{服装,美妆,鞋类,数码}/` 各 1 张**真实产品图**（高清、带真实品牌文字——顺带验各品类文字保真）；FOOD 用现有花生等素材。
- **流程扩一步**（key 恢复后一波跑）：5 品类各出 1 套（≈¥6）→ **质检评图** → **精选入桶 `showcase/` 前缀 + `ShowcaseEntry`+recipe 回填**（**沿 0053 静态清单机制、dev 落码**）→ 首页成果区从纯食品扩成**多品类展示**。
- **展示量控制**：每品类精选 **1–2 张**、总量控制（现 13 张 + 新 4–8 张）；沿 §3.14 ④成果区栅格+懒加载（0054 已上线）自然容纳。
- **口径**：入 showcase 的仍是 `[{url,image_type,caption,recipe}]` 无用户数据/无鉴权（沿 0053）；配方展示仍只露可复用配方、不露内部卡 prompt（0053 铁律）。

## 范围外（YAGNI）
per-品类模板库 / 品类自动识别（用户自选）/ 一次超 4 个品类（按需再增、注册表制）。

## 处理记录
- 2026-07-08 [PM] 用户提需求（coordinator #1031 转达，spec 97c4978 B 线）→ PM 开条挂账。落 PRD §3.18。
  分工：子 agent 出卡草案（coordinator 派，prompt 权威补复核、走卡闸纪律）→ dev（CategoryCardRegistry 注册 4 品类 + category 校验放开 5 值 + chat 工具 schema 同步 + 卡闸 9→13 + openapi 再生，零建表）→ frontend-b（/set 品类选择器默认食品 + codegen，0053 recipe.category 天然兼容）→ QA（②③④先行、①真图等 key）。
  **卡↔code 逐字核对 pytest 闸=硬闸**（改卡须 dev 同步常量、逐字核对，沿卡体系纪律）。真图验收①（5 品类各 1 套 ≈¥6）**排 key 恢复后**，代码/卡先落地。与 A 线（ISSUE-0059）并行、写域不相交。
  status=修复中、owner=提示词（卡）+开发+frontend-b。真实用户 bug 随时打断。
- 2026-07-08 [coordinator] 通报（#1033）：**B 线真图验收素材有着落**——用户将提供 4 品类真实产品图（`image-qa/品类扩展素材/` 按品类分 folder）；
  卡草案子 agent 出稿后 coordinator 把素材路径一并交 QA。→ 验收①（5 品类各 1 套真图、不再「万物皆花生」）用**用户真实产品图**跑（比造图更贴真实场景），仍**排 key 恢复后**。QA 模板「chat 能答新功能」coordinator 已接（固化进 QA 派单标准清单 + QA 子 agent 落 image-qa 模板文件，QA 域自落不越界）。
- 2026-07-08 [PM] **用户追加需求 + 素材就位（coordinator #1035）**：素材就位=`image-qa/品类扩展素材/{服装,美妆,鞋类,数码}/` 各 1 张高清真实产品图（带真实品牌文字→顺带验文字保真）。
  **新增 B5**：品类验收出的图**精选后入首页「看看实朴出的图」成果区**（获客面更宽）——流程扩一步（key 恢复→5 品类各 1 套≈¥6→质检评图→精选入桶 showcase/ + ShowcaseEntry+recipe 回填**沿 0053 静态清单机制 dev 落码**→成果区扩多品类）；每品类精选 1–2 张、总量控制（现 13+新 4–8）；口径沿 0053（无用户数据/无鉴权/配方不露内部 prompt）。验收加⑤（精选图成果区展示正常 + 泄漏扫描干净）。**全部排 key 恢复后一波跑**（真图①⑤同批）。owner 不变（子 agent 卡+dev+frontend-b，showcase 回填 dev 落码）。
- 2026-07-08 [frontend-b] **前端品类选择器完成**（commit `5d05b8d`，纯 image-web 9 文件；提前并行建、非 FOOD 联调等 dev 后端）：
  ① `lib/listing.ts`——新增 `CATEGORIES`(FOOD/FASHION/BEAUTY/SHOES/DIGITAL) + `CATEGORY_LABELS`(食品/服装/美妆/鞋类/数码) + `DEFAULT_CATEGORY`(FOOD) +
     `categoryLabel()`（值→中文·未知原样兼容后端未来新增）；`ListingConfig` 加 `category`(默认食品)；`build{Listing,SetListing}Body` 用 `input.category`
     替硬编码（clone/edit 兜底不变）。② `ConfigSelect` 加可选 `optionLabels`（值→展示名映射、向后兼容）。
  ③ `ListingConfigPanel` 生成设置区顶部加「商品品类」下拉（通栏·5 中文选项·默认食品）；`WorkbenchPage` 把 config.category 串入套图/单图请求。
  ④ **配方复用带品类**：`showcaseRecipeToPrefill` 透传 `recipe.category`（未知回退食品）→做同款带品类；`RecipeFields`/`ShowcaseDetailDialog`
     品类展示复用集中 `categoryLabel`（删各自 {FOOD:食品} 副本、DRY+5 类）——**0053 recipe.category 天然兼容**：非食品配方回显/成果区展示中文标签零改（含 B5 精选入成果区展示天然支持=验收⑤前端侧就绪）。
  **门禁四件套全绿**（lint/tsc/vitest **60** 含 +2 品类透传测试/build）。**Playwright 实证选择器 UI**（验收③前端侧）：/set「商品品类」下拉在位、
     默认**食品**、开列 5 选项(食品/服装/美妆/鞋类/数码)、选「服装」切换生效；category 经 WorkbenchPage 串入请求（单测证 buildBody 透传含非 FOOD FASHION）。
  **待联调**（同波依赖）：非 FOOD 真实生成需 dev 后端 category 校验放开 5 值 + CategoryCardRegistry 注册；**codegen**（category 枚举 5 值）待 dev openapi 再生。
     FOOD 零回归=默认食品、请求体与旧硬编码等价（验收④前端侧）。dev 后端就位 @我 → codegen + 非 FOOD 联调 → QA ②③④ → 部署（真图①⑤等 key）。owner 前端选择器完成、待 dev 后端。
- 2026-07-08 [PM] **B 线后端就位 + 卡稿到位 + DoD 知识库同步 gate 登记**（coordinator #1036 卡稿 `de9219f` / dev #1040 后端 `3f0956c`）：
  ① 卡稿到位=4 新卡（fashion/beauty/shoes/digital 各 `通用.md`，**草案·prompt 权威后补复核**，每卡一物化单元=EDIT 保真块围栏全文）；
  ② dev 后端就位 `3f0956c`=4 品类保真块常量（逐字对齐卡稿）+ CategoryCardRegistry 注册 5 类 + `category` 收为 **Literal 5 值枚举进 openapi**（FOOD/FASHION/BEAUTY/SHOES/DIGITAL）+ 卡闸 pytest 扩 5 品类逐字核对 + openapi 再生 → **frontend-b codegen 可切、非 FOOD 后端已通**；
  ③ ⚠️ **卡稿是草案**——prompt 权威复核若改卡文字→卡闸红→dev re-sync 常量（单一事实源纪律，dev #1040 声明）。
  ④ **DoD 知识库同步 gate（我 2026-07-08 承诺的流程首次触发·dev #1040 提）**：**B 线上线时**知识库 `chat_knowledge.md` 品类行「目前主要支持食品；服装/美妆/鞋类/数码正在扩展中」→ 改「**支持 食品/服装/美妆/鞋类/数码**」。**时点=B 线上线原子同步**（现品类未 live→不提前改，避免 chat 谎称已支持）：B 线部署时 **PM 回写交接源 `image-prd/chat-knowledge-base-v1-draft.md` → dev 落 config 文件**。挂进 B 线上线 checklist、不遗漏。owner 不变（dev 后端就位→待 frontend-b codegen 联调；真图①⑤等 key）。
- 2026-07-08 [frontend-b] **codegen 同步 + 非 FOOD 联调全绿**（commit `fdee26d`；起 dev 3f0956c 后端 mock 实测）：
  ① **codegen**（`fdee26d`）：拉入 dev openapi 的 category Literal 枚举 `"FOOD"|"FASHION"|"BEAUTY"|"SHOES"|"DIGITAL"`——**与前端 `Category` union 逐字一致**，`buildBody` 的 input.category 类型对齐 schema，门禁四件套全绿（lint/tsc/vitest 60/build）。
  ② **非 FOOD 后端联调**（python 复用 0058 加密流程注册登录+上传）：`POST /listing/generate`——**category=FASHION → HTTP 200 job=yes**（非 FOOD 生成后端已通 ✓）/ **category=FOOD → 200**（零回归 ✓，验收④后端侧）/ **category=BOGUS → 422 literal_error**（"Input should be 'FOOD','FASHION',…" = 枚举强校验 ✓）。
  **全链品类路径验证完成**（验收③）：前端选择器(5 选项/默认食品/切换) → config.category → 请求体 category（Playwright UI + 单测透传）→ 后端接受 5 值/拒非法（联调）→ schema 枚举逐字对齐（codegen）。
  **前端份就绪**：选择器 `5d05b8d` + codegen `fdee26d`；与 dev 后端 `3f0956c`（+卡稿 de9219f）**同波原子上**（非 FOOD 前后端一起，否则前端选非 FOOD→后端旧版 422）。
  **待**：真图①（品类特性生效·文字保真）+⑤（精选入成果区）等 key 恢复一波跑（dev showcase 回填落码，我 categoryLabel/recipe.category 展示侧已就绪）。owner 前端 codegen+联调完成 → coordinator 编排 QA ②③④ → 同波部署。
- 2026-07-08 [dev] **B 线后端完成**（`3f0956c`，零建表）：4 品类保真块常量(FASHION/BEAUTY/SHOES/DIGITAL,逐字对齐卡稿 de9219f、
  卡闸 pytest 扩 5 品类逐字核对绿) + CategoryCardRegistry 注册 5 类 + category 收 Literal 5 值枚举进 openapi(frontend-b codegen
  `fdee26d` 逐字对齐、非 FOOD 联调 FASHION→200/FOOD→200/BOGUS→422 全绿)。chat 工具 schema category 随 DTO 自动同步。
  FOOD 零回归。**同波原子上**(后端 3f0956c+卡稿 + 前端 5d05b8d+fdee26d)。真图①⑤等 key。⚠️ 卡稿草案,prompt 权威复核改字→卡闸红→dev re-sync。
- 2026-07-08 [frontend-b] **A/B 线 UI 段验收跑完**（后端 3225b6b@8002·真豆包·mock 图，#1064）：核心全绿——
  ✅ 品类选择器全链（/set 下拉 5 选项默认食品·选美妆→**抓包 POST /listing/generate 请求体精确带 `category:BEAUTY`**·job 200）=验收③选择器+请求段绿；
  ✅ FOOD 零回归（验收④前端侧）。（工具环/带图费用闸/0051 回显=A 线 ISSUE-0059 验收，另绿。）
- 2026-07-08 [PM] **裁决：验收③「配方回显带品类」含历史 job 路径 = (a) 应带、但 fast-follow 不阻断 A/B 波**（frontend-b #1064 亮范围点）：
  **现象**：历史 job「查看配方」不显品类、「复用配置出图」不带品类（回退食品）——因后端 `ListingJobDetailOut`/`ListingJobSummaryOut` **无 category 字段**（仅 showcase `RecipeOut` 有，做同款路径已带品类 ✓）。
  **PM 裁 (a)**：品类既是 first-class config，**历史 job 复用配置应带回原品类**——美妆 job 复用回退食品 = 品类丢失，且可能诱导用户拿食品卡出美妆图（轻度正确性坑，非纯外观）。故验收③**含历史 job 路径**。
  **但不阻断 A/B 波**：核心品类（选+生成非食品套图+请求带 category）已绿、可部署；历史 job 带品类 = **fast-follow 小增量**——⚠️ **dev 先确认 `category` 是否已持久化在 listing_job 上**：是→补 `ListingJobDetailOut`+`ListingJobSummaryOut` category 字段（read-model 暴露、零 DDL）+ frontend 串，记验收③ remainder、随 0057 波或独立小增量跟进；若**未持久化**=同 overlay_texts（ISSUE-0053）先例、升级另议（可能需 DB 变更→签字，届时重估）。owner→dev（确认+补字段）+frontend-b（串）。**0060 代码侧闭环仍待此 remainder + 真图①⑤（等 key）**。（注：coordinator #1067 称此为 backlog，PM 口径精确=**(a) 应带·fast-follow·非丢弃**，验收③未完 0060 不终关。）
- 2026-07-08 [PM] **B 线代码侧已上线 prod（A/B 波 #1067）+ 关账口径**：A/B 波上线 prod（快照 3225b6b、回滚 rollback-20260708-120258、bundle index-ru8hQhu4）；prod 实弹**非法品类 TOYS→422（五值枚举 live）**✓、工作台五品类选择器 live、FOOD 零回归。**代码侧闭环推进**（选择器/请求带 category/卡闸逐字/FOOD 零回归=验收②③选择器段+④绿）。
  **⚠️ 0060 仍不终关——三个 remainder 挂着**：① **真图①⑤**（品类特性生效/文字保真 + 精选入成果区）等 **key 恢复**；② **验收③历史 job 配方带品类** fast-follow（dev 确认持久化+补字段）；③ **DoD 知识库品类行回写**。
  **⚠️ 知识库品类行 flip 时点精确化**：品类选择器虽 code-live，但**输出质量未验**（草案卡未经 prompt 权威复核 + 真图未跑=等 key）→ 知识库品类行**暂不翻**「正在扩展中」→「支持 5 品类」，**gate=真图验收绿 + prompt 卡复核（品类可用性正式公告）**，非 code-deploy。理由：现翻=chat 谎称已支持但输出可能差（草案卡）；under-promise 更安全（内测灰度）。真图验收绿那刻我回写 draft→dev 落 config。owner=提示词（卡复核）+开发（真图后 showcase 回填/DetailOut category）+frontend-b（历史品类串）。
