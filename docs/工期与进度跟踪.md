# 设计中台 · 工期与进度跟踪

> 本文件是**活文档**：每完成一项里程碑，更新「状态」「完成日期」并在文末「进度日志」追加一条（含提交短 SHA）。
> 依据：`image-prd/2026-05-27-design-platform-prd.md` + `docs/feature-breakdown/2026-05-28-stage1-engine-core-feature-points.md`。
> 估时口径：1 名熟练后端工程师有效工时（人天），已含 TDD/自检开销；前端/阶段2 为粗估，开工前细化。
> 图例：✅ 已完成 · 🔨 进行中 · ⏳ 待办

| 最近更新 | 2026-06-01 |
|---|---|

---

## 一、总览

### 阶段 1：图生图引擎（命脉）

| # | 里程碑 | 内容 | 估时(人天) | 状态 | 完成日期 |
|---|---|---|---|---|---|
| 1 | **M1 架构脊柱** | Provider 适配/路由/Prompt 编排(4族+5词库)/cost-guard/pipeline，全 Mock 跑通 | 18 | ✅ | 2026-05-28 |
| 2 | **六边形重构** | 重组为 Ports & Adapters（domain/ports/application/infrastructure/config），绝对导入 | 1 | ✅ | 2026-05-28 |
| 3 | **interface/API 骨架** | FastAPI 可运行服务：`POST /generate`、`/generate/cost-preview` | 1 | ✅ | 2026-05-28 |
| 4 | **M2a 持久化** | SQLAlchemy async ORM（10 表）+ Alembic + Ledger/Job 仓储（方言无关，MySQL 落地） | 2 | ✅ | 2026-05-28 |
| 5 | **M2b 异步** | arq worker + Redis 事件 + SSE 进度 + 异步端点，接入 MySQL 落库 | 3 | ✅ | 2026-05-28 |
| 6 | **M3 真实 Provider** | OpenAI/千问·万相/Seedream/灵动 4 家对接 + qwen-vl 视觉（注册即用，OCP） | 6 | 🔨 | — |
| 7 | **M4 两阶段合成（命脉）** | BiRefNet 抠图 + 程序合成 + 光影协调 + S4 多角度双路径 | 9.5 | ⏳ | — |
| 8 | 认证授权 | 飞书/钉钉 OAuth + JWT + 角色权限矩阵 | 2 | ⏳ | — |
| 9 | 监控告警 | Prometheus + Grafana(4 面板) + Sentry + structlog 埋点 | 3 | ⏳ | — |
| 10 | 模型配置后台 | ModelConfig CRUD + 单价从 DB 注入 Provider（热更，替换写死 Mock 价） | 2 | ✅ | 2026-06-01 |

**阶段 1 已完成 ≈ 25 人天 / 待办 ≈ 22.5 人天。**

### 阶段 2：副驾驶链路（叠加在阶段 1 上）

| # | 里程碑 | 内容 | 估时(人天) | 状态 | 完成日期 |
|---|---|---|---|---|---|
| 11 | 项目工作台 | 4 态状态机、一单一档、轮次管理（ORM 表已就绪） | 3 | ⏳ | — |
| 12 | 标准化需求单 | 8 字段表单 + 字段→Prompt 自动注入 | 2 | ⏳ | — |
| 13 | 改稿单 | 手动添加、逐条勾选、关联生成图 | 2 | ⏳ | — |
| 14 | 客户档案 | 6 字段维护 + 历史项目关联 + 复用 | 1.5 | ⏳ | — |
| 15 | 导出归档 | 多格式(JPG/PNG/PDF) + 批量改尺寸 + zip + 命名规范 + 文件夹归档 | 3 | ✅ | 2026-06-01 |

### 前端与交付（贯穿）

| # | 里程碑 | 内容 | 估时(人天) | 状态 | 完成日期 |
|---|---|---|---|---|---|
| 16 | 前端工作台 | Vite+React19+shadcn/ui+TanStack Query+Zustand；选稿界面 C1、recharts 业务仪表盘、EventSource 接 SSE | 15 | ⏳ | — |
| 17 | 部署与 CI/CD | Docker Compose + nginx + GitHub Actions + OSS/KMS/WAL-G | 4 | ⏳ | — |

---

## 二、关键路径与并行

- **关键路径**：M1 → M3/M4（M1 的 SOLID 边界决定后续可插入式扩展）。
- **当前可并行**（M1/M2 已稳定，按 OCP/DIP 解耦）：
  - M3 真实 Provider（只往 `infrastructure/providers/` 加文件，不动 application）
  - M4 两阶段合成（新增 `infrastructure/composition/`，pipeline 通过端口调用）
  - 阶段 2 各模块（ORM 表已就绪，补仓储 + 用例 + 路由）
- **依赖提醒**：M4 的 S4 单角度兜底依赖 M3 的多视角模型 Provider 就绪。

---

## 三、已完成里程碑详情（本会话，2026-05-28）

- **架构**：六边形 / Ports & Adapters，依赖一律指向内 `domain ← ports ← application ← infrastructure`；统一绝对导入；公共契约经 `__init__` 暴露。
- **质量门禁**：ruff clean + mypy strict（74 文件）全绿；全程 SOLID 标注。
- **验证方式**：Dev 角色不写测试用例（归 QA/image-qa），用 `uv run python -c` smoke 自检 + 真实 MySQL/Redis e2e。
- **真实基础设施**：本地 MySQL 8.0（`image_gen`，10 表）+ Redis 8.0；密钥全程环境变量、未入库。

---

## 四、进度日志（append-only）

| 日期 | 里程碑 | 说明 | 提交 |
|---|---|---|---|
| 2026-05-28 | M1 架构脊柱 | Provider 适配/路由/Prompt 编排/cost-guard/pipeline，Mock 端到端 | 88e58af 14b6ed1 42a6f8f fdf01e4 46c009a |
| 2026-05-28 | 六边形重构 | 重组分层 + 绝对导入 + 清理误入库字节码 | a045b5a e6f4975 |
| 2026-05-28 | interface/API 骨架 | FastAPI 同步出图 + 成本预估端点；同步计划文档 | b34b647 25b1349 |
| 2026-05-28 | M2a 持久化 | SQLAlchemy async ORM(10 表)+Alembic+Ledger/Job 仓储，MySQL 落地 | 975a231 |
| 2026-05-28 | M2b 异步 | 事件端口+Redis/内存总线+任务编排；arq worker+异步端点+SSE，真实 e2e | 39cda83 d931d2f |
| 2026-05-28 | M3 进行中 | gpt-image 中转 adapter(OpenAI 标准协议) + 主备双中转 failover(DIP)，httpx MockTransport 零成本验证；真实调用待密钥/中转站确认 | (本次) |
| 2026-05-29 | 提示词三维差异化 | 品类画像(数码/食品/美妆/服饰 4)+风格预设(国潮/简约/轻奢/科技/清新/运动 6)正交接入 orchestrator；光影=光型(品类)+光色(风格)拼接；修比例重复/质量词冗余；清理 Color/Guard/Lens 词库；预留图生图 EDIT 保真接口 | (Task1-6) |
| 2026-05-29 | M3 图生图打通 | 补 /images/edits(multipart+b64)+ImageStore 端口(本地落点 A)；闭合 ISSUE-0002 五项(错误 status_code 分流/failover 取最贵/同模型断言/超时180s/trust_env 直连)；**apinebula 真实联调 1 张出 1024² PNG ~¥0.10**。待接进 build_engine 路由 | (M3 图生图) |
| 2026-05-29 | M3 接线/generate 走真实图 | build_registry(real_gpt_image) 按 name 覆盖 GPT_IMAGE_2 为真实 Provider(LSP)；pipeline 按参考图推断 EDIT/TEXT2IMG；asgi+worker 路由真实 gpt-image。**真实端到端验证**：占位输入 3 张 + 真实棕色玻璃瓶 1 张(美妆×轻奢, 瓶身保真+换景+文案排版)，各 ~¥0.10。其余模型仍 Mock(无 key) | (接线提交) |
| 2026-06-01 | 阶段2 WP-A 项目工作台 | 客户/项目仓储端口+SQLAlchemy实现+用例+薄路由；4态状态机(数据驱动转移表,非法流转→409,审稿⇌设计中改稿轮次+1)；asgi 装配 DB-backed service+路由聚合(给后续 WP 留位)；DomainError→409 边界映射；未动 schema。验证 ruff+mypy(103)+TestClient sqlite 全链路 | WP-A 2 提交 |
| 2026-06-01 | ⑥ WP-F 成本仪表盘 | 5 维聚合**只读** API：`GET /dashboard/cost?dim=overview\|model\|project\|designer\|tier&period=month\|week\|day`。CQRS 读侧：新建 `CostQueryPort`(DIP,独立端口不碰 repositories.py) + `SqlAlchemyCostQuery`(func.sum/count/group_by, generated_image⋈generation_job) + `CostReportService`(业务衍生:成本占比/单张均价/达标率/档位 70-25-5 目标对照) + 薄路由 + asgi 装配。口径:成本=generated_image.cost(已实现单张成本,ledger 无 model/project/tier 维度故专司预算);达标率=评分≥4星/已评分(PRD §1.2/§3.11)。未动 schema。验证 ruff+mypy(114)+sqlite 内存库 5 维手算数值 smoke 全绿。**并发协作**:源码被并发 WP-B 全量 add 扫入 80dcf40;集成自检发现并开 ISSUE-0004(AssetRepository.list 遮蔽内置 list→asgi 无法 import,owner=WP-B,P1) | 80dcf40(源码) + 本次(docs/issue) |
| 2026-06-01 | ⑫ WP-B 需求单+素材+项目下出图 | D2 方案①落地：需求单(brief 表,PRD 8 字段,PUT 全量替换语义)+素材 multipart 上传(新增 AssetStore 端口 save+load,与只写 ImageStore 区分;LocalAssetStore 本地读写)+`brief_to_brief.compose_brief` 合成域 Brief(需求单 copy_text/taboo + 出图配置 GenerationConfig 单值 family/品类/子场景/档位/尺寸/风格 + 选中素材字节)+项目下**同步**出图(复用 GenerationPipeline,落库挂 project_id+当前 round_no,一单可 N 次出图)。新增 NotFoundError→404 边界映射。校验:项目/需求单不存在→404,跨项目素材/空文件→400,n>12/未实现族→400。未动 DB schema(brief/asset/generation_job 表已就绪)。验证 ruff+mypy(119)+sqlite 内存库 仓储/应用层/HTTP(httpx ASGITransport) 三层 smoke 全绿。异步项目出图待补(见接口清单 §6)。**协作**:修复 ISSUE-0004(from __future__ import annotations 根治 list 遮蔽);早期两次 `git add -A` 误卷 WP-F 源码(80dcf40)/issue 文件,后续已改用显式路径提交 | 80dcf40 ad5b114 af33a3b 6960f1d c87cac8 + 本次(docs) |
| 2026-06-01 | ⑧ WP-H 模型配置后台 | ModelConfig CRUD + 单价热更注入：新建 `ModelConfigRepository` 端口(独立文件,不碰 repositories.py)+SqlAlchemy 实现(update 缺失→NotFoundError 404;seed_defaults 仅插缺失,幂等不覆盖改价)+`ModelConfigService`(list/update[负价→ValueError 400]/seed_defaults/unit_cost_map)+薄路由 `GET /admin/models`·`PUT /admin/models/{name}`(鉴权留 TODO 待 WP-G)。**单价注入**(composition,向后兼容 worker):build_mock_registry/build_gpt_image_provider/build_registry/build_engine 增 `unit_costs` 形参(默认 None=Mock 兜底),costs={**_MOCK,**DB} 合并缺失回落;asgi lifespan seed 5 模型+读 DB 单价注入 registry(pipeline/preview 构造前)。单价真实来源转 model_config 表,替换写死 `_MOCK_UNIT_COSTS`(保留兜底)。enabled 暂作元数据(路由消费 model_config 属后续)。未动 schema(表已存在)。验证 ruff+mypy(133)+sqlite 内存库 smoke 全绿:seed/幂等+CRUD+估算读到 DB 新价(0.99×6=5.94)+Mock 兜底+404/400。**协作**:asgi.py 为多 WP 共写热点,本次提交同时携带并行 WP-C(选稿评分)工作区未提交的 selection 接线(其源码已在 HEAD,按"保留双方各自的行"未回退) | 621ce68 + 本次(docs) |
| 2026-06-01 | ① WP-C 选稿+评分 | `GET /jobs/{id}/images`、`POST .../images/{image_id}/score`(分值1-5)、`POST .../keep`、`GET /jobs/{id}/usable-rate`(≥4星=可用,rate=usable/total)。独立端口 `ports/image_repository.py`(避开 repositories.py;加 from __future__ import annotations 防内置名遮蔽)+SqlAlchemy 实现+`SelectionService`(校验分值1-5、校验图归属本任务)+独立 schemas/deps/薄路由。分值非法→ValueError 400、任务/图不存在→NotFoundError 404。未动 schema(generated_image 已有 score/kept)。验证 ruff+mypy+sqlite 内存库 仓储/用例/HTTP(ASGITransport) 三层 smoke 全绿。**协作**:端口/仓储/用例/接口分 3 次独立提交;asgi.py 与并行 WP-H(admin) 交错且一度硬依赖 WP-H 未提交模块,故 WP-C 装配行就位但不单独提交(免破坏 HEAD/扫入他人代码),开 ISSUE-0005;**WP-H 已合并提交 asgi(含 WP-C selection 行)并复验,ISSUE-0005 已修复** | c6a096e c001336 0956b6b（+asgi 随 621ce68 合并）+ 本次(docs) |
| 2026-06-01 | ⑮ WP-E 导出归档 | `POST /projects/{id}/export`(多格式 JPG/PNG/PDF + 批量改尺寸 + zip + 命名规范 + 文件夹归档)、`POST /images/{image_id}/resize`(单图改尺寸)。六边形/DIP：新端口 `ports/exporter.py`(Exporter 纯变换 + ExportStore 读源/结构化写出 + ExportQuery 读侧元数据)+`infrastructure/export/*`(PillowExporter[JPG/PDF 拍平 RGB]、naming[PRD §4.4 命名+归档目录纯函数]、packager[内存 zip]、local_export_store[读 file://、防目录穿越])+`infrastructure/db/export_query.py`(generated_image⋈job 取 customer/subscene/tier/round + 候选序号)+`ExportService`(编排,pillow 变换走 asyncio.to_thread)+薄路由。命名 `P{id:04d}_客户_场景_轮次N_候选N_档位_时间戳.ext`(project_code 列待 D3,暂用 id)、归档目录 项目/子场景/轮次。图不存在→NotFoundError 404、跨项目/参数非法→ValueError 400。`uv add pillow`(12.2.0,CLI;uv.lock 随提交)。新增 settings.export_output_dir。未动 schema。验证 ruff+mypy(隔离 WP-D 在途改动后 152 文件全绿)+sqlite 内存库真实 PNG smoke 全绿(三格式/命名/归档目录/改尺寸 100×50/zip/单图 resize 120×90/404/400)。**协作**:asgi.py:102 现因并行 WP-D 未提交的 ProjectService(+revisions) 改动报 mypy 红,属 WP-D 在途残缺(HEAD 自洽绿);本次用 `git stash` 临时隔离 WP-D 改动验证+提交后 pop 还原,显式 add 含 uv.lock,未碰 WP-D 文件 | 4756778 + 本次(docs) |
| 2026-06-01 | ⑬ WP-D 改稿单 | 开/列/加条目/逐条勾选(`POST`/`GET /projects/{id}/revisions`、`POST /revisions/{rid}/items`、`PUT /revisions/{rid}/items/{seq}`) + **交付强校验**(在 `PUT /projects/{id}/status`→已交付 触发:有未完成改稿条目→DomainError 409,管理者 ?force=true 跳过)。独立端口 `ports/revision_repository.py`(避开 repositories.py;from __future__ import annotations)+SqlAlchemy 实现(items JSON 整体重赋值触发脏检测;status 随条目派生)+`RevisionService`+独立 schemas/deps/薄路由;扩展 `ProjectService` 注入 RevisionRepository + transition force(改 project_service.py+routes/projects.py+asgi,三文件原子提交)。空文本→400,单/条目不存在→404。未动 schema(revision 表已有 items JSON)。验证 ruff+mypy(152)+sqlite 内存库 仓储/用例/HTTP 全链路 smoke 全绿。**协作**:project_service.py/routes 未提交改动一度被并行工作树重置抹掉→改为尽快提交保护;asgi 一度阻塞(依赖 WP-E 未提交模块),WP-E 提交 4756778 后解除,WP-D 装配干净接入 | b2898b7 c52daa3 5a52cd1 b4033cd + 本次(docs) |
