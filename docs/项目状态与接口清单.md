# 设计中台 · 项目状态与接口清单（并行协调看板）

> 用途：**多 agent 并行施工的交接看板**。新窗口零上下文也能照此领工作包。
> 配套：时间线见 `docs/工期与进度跟踪.md`；设计依据见 `image-prd/` 与 `docs/superpowers/specs/`。
> 最近更新：2026-06-01。代码根：`image-code/`（包 `design_hub`）。

---

## 0. 一分钟全景

- **是什么**：设计师 AI 副驾驶中台。核心引擎 = 产品图 → 高质量电商图（图生图）。
- **架构**：六边形（Ports & Adapters），依赖一律指向内 `domain ← ports ← application ← infrastructure ← interface`。绝对导入。
- **技术栈**：Python 3.12 · uv · FastAPI · SQLAlchemy 2.0 async · Alembic · arq · Redis · MySQL(本地, PRD 原定 PG) · httpx。
- **质量门禁**：`uv run ruff check src` + `uv run mypy`(strict) 必须全绿。
- **铁律**：SOLID 强制；Dev 角色**不写测试**(测试归 image-qa)；**涉及 DB 先问用户**；密钥只走 `.env`(gitignored) 不入库；完成一单元即 commit。
- **进度概数**：出图引擎后端 ≈ 完成；中台业务后端 ≈ 3-4 成(WP-A/B/F 已交付)；前端 = 0%。整体剩余 ≈ 35 人天(砍 M4 后 ≈26；不含前端 ≈17)。

---

## 1. 已完成 ✅（全部已提交 git，可运行可验证）

| 模块 | 内容 | 落点 |
|---|---|---|
| M1 架构脊柱 | 领域枚举/DTO、Provider 抽象+Mock+注册表、模型路由(族×档位)、Prompt 编排、cost-guard、GenerationPipeline | `domain/ ports/ application/` |
| 六边形重构 | 分层 + 绝对导入 + 公共契约 `__init__` 暴露 | 全局 |
| interface/API | 同步出图 + 成本预估端点；create_app 工厂 + 边界错误映射 | `interface/api/` |
| M2a 持久化 | SQLAlchemy async ORM **10 表** + Alembic 迁移 + Ledger/Job 仓储；本地 MySQL `image_gen` 已建表 | `infrastructure/db/ infrastructure/ledger/` |
| M2b 异步 | 事件端口 + Redis/内存总线 + 任务编排 + arq worker + 异步端点 + SSE | `infrastructure/events/ infrastructure/queue/ application/task_runner.py` |
| 提示词三维 | 品类画像(数码/食品/美妆/服饰 4) × 风格预设(国潮/简约/轻奢/科技/清新/运动 6) × 模板族(3/4/5/7)；光型(品类)+光色(风格)拼接 | `application/prompt/profiles/` |
| M3 图生图 | `/images/edits`(multipart+b64) + ImageStore 端口 + 本地落点；ISSUE-0002 五项闭合；**真实 apinebula gpt-image-2-vip 出图打通**(~¥0.10/张, 1024² PNG)；`/generate` 已路由真实图 | `infrastructure/providers/openai_compat.py` `infrastructure/storage/` `composition.py` |

**真实基础设施已联通**：本地 MySQL 8.0 + Redis 8.0；apinebula 中转(图生图实测出图)。

---

## 2. 未完成 ⏳（按里程碑，人天为单人有效工时）

| 模块 | PRD | 人天 | 依赖 | 备注 |
|---|---|---|---|---|
| ~~阶段2 项目工作台(客户/项目/状态机/轮次)~~ ✅ **WP-A 完成** | ⑪ | ~~3~~ 0 | — | 客户/项目 CRUD+4态状态机已上 asgi |
| ~~阶段2 标准化需求单+素材+项目下出图~~ ✅ **WP-B 完成** | ⑫ | ~~2~~ 0 | — | 需求单 PUT/GET + 素材 multipart + 项目下同步出图(挂 project_id+round_no)；D2 方案① brief_to_brief 合成 |
| 阶段2 改稿单(逐条勾选/关联图/强校验) | ⑬ | 2 | 工作台 | |
| 阶段2 客户档案(6字段+跨项目复用) | ⑭ | 1.5 | 工作台 | |
| 阶段2 导出归档(多格式/批量改尺寸/zip/命名) | ⑮ | 3 | 需 Pillow | |
| 选稿+评分(C1, 保留/4星打分→首次可用率) | ① | 1.5 | ORM 有 score/kept 列 | |
| ~~成本仪表盘(5维报表聚合)~~ ✅ **WP-F 完成** | ⑥ | ~~2~~ 0 | — | 5维只读API(overview/model/project/designer/tier × month/week/day)已上 asgi |
| 认证授权(飞书/钉钉 OAuth+JWT+角色矩阵) | ⑨ | 2 | | 端点现全裸 |
| 模型配置后台(ModelConfig 热更) | ⑧ | 2 | ModelConfig 表已有 | |
| 其余真实 Provider(千问/Seedream/灵动/qwen-vl) | ① | 4 | **需 key+预算** | OCP 加适配器即可 |
| 监控告警(Prometheus/Grafana/Sentry 埋点) | ⑩ | 3 | | |
| M4 两阶段合成(BiRefNet 抠图+合成+光影) | ③ | 9.5 | GPU/权重 | **建议先砍**：图生图端点已保真 |
| 前端工作台(Vite+React19+shadcn+recharts+SSE) | — | 15 | 后端接口 | 0%，独立大头 |
| 部署 CI/CD(Docker Compose+nginx+OSS/KMS) | — | 4 | | |

---

## 3. 后端接口清单

### 3.1 已实现（4 个，已接真实 gpt-image）

> 公共请求头：`X-User-Id: <设计师标识>`（默认 `designer-anon`）。
> 同步应用 `design_hub.interface.api.app:app`（全 Mock）；生产应用 `design_hub.interface.api.asgi:app`（真实 MySQL+Redis+gpt-image）。

**`POST /generate`** — 同步出图
```jsonc
// Request (GenerateRequest)
{
  "customer": "客户A",
  "subscene": "S1",            // S1换背景 | S3场景图 | S4多角度
  "family": "family_4",        // family_3/4/5/7 (V1)
  "tier": "standard",          // draft | standard | refine
  "style": "高端轻奢",          // 国潮中式|极简北欧|高端轻奢|科技未来|清新自然|运动机能
  "category": "3C数码",         // 3C数码|服饰配件|美妆护肤|食品 (V1有画像)
  "width": 1024, "height": 1536,
  "n": 6,                      // 1..12
  "copy_text": "焕新水光",       // 可选
  "taboo": null, "product_desc": null, "brand_name": null
}
// Response (GenerateResponse)
{
  "used_model": "gpt-image-2",
  "total_cost": "1.20",
  "positive_prompt": "…", "negative_prompt": "…",
  "images": [{"url": "...", "seed": 100, "latency_ms": 80000, "cost": "0.10"}]
}
```
> 备注：扁平 `/generate` 的 HTTP schema 仍无图片上传字段；**图生图经 HTTP 走通已由 WP-B 提供**——`POST /projects/{id}/assets` 上传素材 + `POST /projects/{id}/generate` 传 `asset_ids[]`，pipeline 自动切 EDIT 模式。

**`POST /generate/cost-preview`** — 出图前成本预估（只读不预扣）。请求同上，返回 `{tier, candidate_count, model, estimated_cost, month_used, month_budget}`。

**`POST /generate/async`** — 入队，返回 `{"job_id": "<hex>"}`。请求同 `/generate`。

**`GET /generate/{job_id}/events`** — SSE 进度流。事件：`task_started / model_called / image_generated / task_completed / task_failed`，`data` 为 JSON。

**边界错误映射**：`BudgetExceeded→409` / `ValueError→400` / `KeyError(未实现族/品类)→400` / `ProviderError→502`。

### 3.2 规划中（建议 REST 契约，供并行开发对齐；未实现）

> 命名建议；实现前每块走各自 spec。一单一档下出图应挂项目。

| 端点 | 作用 | 工作包 |
|---|---|---|
| ✅ `POST /customers` `GET /customers` `GET /customers/{id}` | 客户档案 CRUD（**WP-A 已实现**, asgi 装配） | WP-A |
| `GET /customers/{id}/projects` | 客户历史项目（用 `GET /projects?customer_id=` 暂代） | WP-A |
| ✅ `POST /projects` `GET /projects[?customer_id=]` `GET /projects/{id}` | 项目 CRUD（**WP-A 已实现**） | WP-A |
| ✅ `PUT /projects/{id}/status` | 4 态状态机流转（**WP-A 已实现**, 非法流转→409, 审稿→设计中自动轮次+1） | WP-A |
| ✅ `PUT /projects/{id}/brief` `GET /projects/{id}/brief` | 8 字段需求单(**WP-B 已实现**；项目/需求单不存在→404) | WP-B |
| ✅ `POST /projects/{id}/assets` `GET /projects/{id}/assets` | 上传/列 产品图·参考图(multipart, **WP-B 已实现**；空文件→400) | WP-B |
| ✅ `POST /projects/{id}/generate` | 项目下同步出图(出图配置→合成域 Brief→挂 project_id+round_no, **WP-B 已实现**)；async 待补(见 §6) | WP-B |
| `POST /jobs/{job_id}/images/{image_id}/score` `…/keep` | 选稿+评分(4星/保留) | WP-C |
| `POST /projects/{id}/revisions` `PUT /revisions/{id}/items` | 改稿单(逐条勾选/关联图/强校验) | WP-D |
| `POST /projects/{id}/export` | 导出(多格式/批量改尺寸/zip/命名规范) | WP-E |
| ✅ `GET /dashboard/cost?dim=overview\|model\|project\|designer\|tier&period=month\|week\|day` | 成本仪表盘 5 维(**WP-F 已实现**, asgi 装配) | WP-F |
| `POST /auth/{feishu\|dingtalk}/callback` `GET /me` | OAuth + JWT + 角色 | WP-G |
| `GET /admin/models` `PUT /admin/models/{name}` | 模型配置热更(管理者) | WP-H |

### 3.3 数据模型（10 张表，已建已迁移）

`customer`(name/contact/industry/brand_color/common_styles·taboos·sizes JSON) · `project`(customer_id/name/status/current_round) · `brief`(project_id/material_types·sizes·styles JSON/resolution/bleed/copy_text/taboo/delivery JSON) · `asset`(project_id/kind/url) · `generation_job`(id str/project_id 可空/user_id/customer/subscene/family/tier/style/category/width/height/candidate_count/used_model/total_cost/positive·negative_prompt/status/round_no/extra JSON) · `generated_image`(job_id/url/seed/latency_ms/cost/score/kept) · `revision`(project_id/round_no/items JSON/status/deadline) · `deliverable`(project_id/fmt/files JSON) · `model_config`(name PK/unit_cost/enabled/extra) · `cost_ledger`(user_id/amount±/created_at, append-only)。

---

## 4. 前端接口/页面清单（0%，全待建）

> 前端独立项目（建议新目录 `image-web/`，非 image-code）。技术栈 PRD §6.1：Vite+React19+shadcn/ui+Tailwind+TanStack Query+Zustand+recharts+EventSource。

| 页面 | 消费的后端接口 | PRD | 工作包 |
|---|---|---|---|
| 登录 | `/auth/*` `/me` | ⑨ | WP-J |
| 项目工作台(一单一档) | `/projects*` `/customers*` `/projects/{id}/brief` | ⑪⑫ | WP-J |
| 出图 + 成本预估弹窗 | `/projects/{id}/generate` `/cost-preview` + SSE `/events` | ④⑥ | WP-J |
| 选稿界面(6 候选缩略图+评分) | `/jobs/{id}/images/*/score\|keep` | ⑤ | WP-J |
| 改稿单 | `/projects/{id}/revisions` | ⑬ | WP-J |
| 交付/导出 | `/projects/{id}/export` | ⑮ | WP-J |
| 业务仪表盘(recharts) | `/dashboard/cost*` | ⑥ | WP-J |
| 管理后台(模型配置/预算) | `/admin/models*` | ⑧ | WP-J |

---

## 5. 并行工作包（多 agent 协调，含写边界/依赖）

> **写边界铁律**（父 `CLAUDE.md`）：每个角色只写自己主文件夹 + `image-issues`。开发类工作包都在 `image-code/`，**多 agent 同写 image-code 需避免改同一文件**——下面按"独立文件/子包"切分，标注冲突风险。
> 每个 WP 交付都要：ruff+mypy 绿 + smoke 自检 + commit；DB 相关先问用户。

| 包 | 范围 | 主要新增文件(避免互撞) | 依赖 | 可并行? |
|---|---|---|---|---|
| **WP-A** 客户+项目+状态机 | 仓储+用例+路由 | `ports/repositories.py`(新)、`infrastructure/db/customer_repo.py`/`project_repo.py`、`application/project/*`、`interface/api/routes/projects.py` | ORM 已就绪 | ✅ 基座，建议先做 |
| ✅ **WP-B** 需求单+素材+项目下出图(**完成**) | brief upsert/get + 素材上传(multipart) + brief_to_brief 合成 + 挂项目同步出图 | `ports/asset_store.py`(新端口)、`infrastructure/storage/local_asset.py`、`infrastructure/db/{brief,asset}_repo.py`、`application/project/{brief_service,asset_service,brief_to_brief,project_generation_service}.py`、`routes/brief.py`、`project_schemas.py`扩展 | WP-A✅+D2✅ | ✅ 已交付 |
| **WP-C** 选稿+评分 | image score/keep 用例+路由 | `application/selection/*`、`infrastructure/db/image_repo.py`、`routes/selection.py` | generation_job 落库(已有) | ✅ |
| **WP-D** 改稿单 | revision 用例+逐条勾选+交付强校验 | `application/revision/*`、`routes/revision.py` | WP-A | 🟡 |
| **WP-E** 导出归档 | 多格式/批量改尺寸/zip/命名规范 | `ports/exporter.py`、`infrastructure/export/*`(需 Pillow) | generated_image | ✅ 独立 |
| ✅ **WP-F** 成本仪表盘(**完成**) | 5 维聚合查询(只读) | `ports/cost_query.py`(新端口)、`infrastructure/db/cost_query.py`、`application/dashboard/*`、`interface/dashboard_schemas.py`+`api/dashboard_deps.py`+`routes/dashboard.py` | generated_image⋈job | ✅ 已交付 |
| **WP-G** 认证授权 | OAuth+JWT+角色中间件 | `ports/auth.py`、`infrastructure/auth/*`、`interface/api/deps.py` 扩展 | — | ✅(但改 deps 需协调) |
| **WP-H** 模型配置后台 | ModelConfig CRUD + 路由表从 DB 读 | `infrastructure/db/model_config_repo.py`、`routes/admin.py` | model_config 表 | ✅ |
| **WP-I** 其余真实 Provider | 千问/Seedream/灵动/qwen-vl 适配器 | `infrastructure/providers/{dashscope,volcengine,lingdong}.py`、`infrastructure/vision/qwen_vl.py` | **需 key+预算** | ✅(独立文件) |
| **WP-J** 前端 | 整个前端 | `image-web/`(新目录) | 后端接口契约(§3) | ✅ 完全独立 |
| **WP-K** 提示词优化(研究) | prompt 瘦身对照 + 带Logo产品保真测试 | 实验脚本(不入产品码) | 真实 key+预算 | ✅ 研究类 |

**推荐并行批次**：
- **第一批(无依赖, 可同时开)**：WP-A、WP-E、WP-F、WP-H、WP-J(前端先搭骨架按契约 mock)
- **第二批(依赖 WP-A)**：WP-B、WP-C、WP-D
- **需用户给 key/预算**：WP-I、WP-K
- **建议先砍**：M4 两阶段合成

---

## 6. 待对齐/待补（开工前需解决）

- ✅ **D2(核心矛盾, 已定+落地)**：方案①「需求单 + 出图配置分离」——需求单存 PRD 8 字段(多选)，出图时单独提交 `GenerationConfig`(单值 family/品类/子场景/档位/尺寸/风格 + asset_ids)，`application/project/brief_to_brief.compose_brief` 合成域 Brief。**WP-B 已实现**。
- **D3**：项目编号——PRD 用人类可读码 `P20260527-001`，当前 ORM 是自增 int。建议加 `project_code` 列(WP-A 处理)。
- ✅ **D4(已闭合)**：`POST /projects/{id}/generate` 落库挂 `project_id`+`round_no`(项目当前轮次)；脱离项目的扁平 `/generate` 仍可独立出图(round_no 默认 1)。
- **D5**：M4 命脉重估——图生图 `/images/edits` 已保真，建议砍/缩 M4。
- **D6**：成本——gpt-image 图生图实扣 ~¥0.10-0.44/张，非 PRD 的 1-3 毛(已知)。
- ✅ **HTTP 图片上传(已补)**：`POST /projects/{id}/assets`(multipart) 上传素材 + `/projects/{id}/generate` 传 `asset_ids[]` → pipeline 自动 EDIT 图生图。扁平 `/generate` 仍无上传字段(按需再补)。
- **项目下异步出图(待补)**：`POST /projects/{id}/generate/async` 暂未实现——队列 payload 刻意不携带素材字节，需 worker 按 `asset_ids` 重组域 Brief(改 payload/worker/task_runner)；当前仅同步 `POST /projects/{id}/generate`。
- **MySQL vs PG(D1)**：方言无关 ORM，已用 MySQL，PG 后切只改连接串。

---

## 7. 跑起来 / 验证（给新 agent）

```bash
cd image-code
uv sync
uv run ruff check src && uv run mypy          # 门禁
# 同步全 Mock 服务(零基础设施)：
uv run uvicorn design_hub.interface.api.app:app
# 真实服务(需本地 MySQL+Redis+.env 配 GPT_IMAGE_*)：
uv run arq design_hub.infrastructure.queue.worker.WorkerSettings   # worker
uv run uvicorn design_hub.interface.api.asgi:app                   # api
# 迁移：
uv run alembic upgrade head        # DB_URL 经环境变量
```
- `.env`(gitignored) 配：`DB_URL`、`REDIS_URL`、`GPT_IMAGE_BASE_URL/API_KEY/MODEL`。境内中转 `trust_env=False` 已内置。
- 角色协作：`image-issues/` 黑板开条目流转；Dev 不写测试(归 QA)；DB 改动先问用户。

---

**文档结束。** 这是并行施工的单一事实源；各 WP 推进时更新本表「未完成/接口」两节 + `工期与进度跟踪.md` 日志。
