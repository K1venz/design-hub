# 二次编辑 · 编辑模式提示词层草案(ISSUE-0040 · 方案稿,未动代码)

> 起点:PRD §3.12.13 定稿、schema 三列已签上线(parent_job_id/source_image_key/edit_mode{delta,full})、用户早批 delta+full。
> 状态:备稿,待 PM 摘要落地后进三方对。

## 1. 与首次出图的本质差异:源图语义反转

首次出图:参考图 = **真实产品照**,保真块说"保留产品、重绘周围"。
二次编辑:参考图 = **上一次的生成结果**(按 source_image_key 从 TOS 读)——它已含场景/构图/产品,用户要"在此基础上改"。

→ **通用品类保真块在编辑模式下语义冲突**:它的"只重绘产品周围的背景、道具与光线"正是 delta 要锚定不动的东西(或恰是用户要定向改的东西)。
**提案:编辑模式不注入品类保真块,改用自含产品保真语义的「编辑模式块」**(同复刻模式独立成层的先例)。

## 2. 两模式块(第六类卡:编辑模式卡,沿物化块协议)

**双图角色(Q1 定案后的统一输入,喂序已与 dev #635 锁定)**:**第 1 张 = 被编辑源图**(编辑基底——edits 的第一张是主编辑对象,delta 的主对象正是源图画面)、**其后 1..3 张 = 迭代链根原始产品图**(产品保真锚,零代损耗;"其后所有"措辞天然覆盖可变张数、零槽位)。两模式块开头做角色指认(沿复刻"序=契约"先例,dev 保序)。

### delta · 微调(默认)
- **全锚定 + 点改**:构图、场景、光线与一切未点名元素**保持与第 1 张(源图基底)一致**;仅按用户指令做**最小幅度定向调整**。
- 产品的外形、包装与文字**以其后的产品原图为准逐字逐细节保留**——产品保真锚链根,防漂移累积。
- 命门:①改动外溢(模型把"换背景"做成"重做整张")——锚定句权重最前;②产品漂移——双图锚治本(Q1);③用户指令要求改产品本体——"产品不变"在前压制(产品级改动超出二次编辑 scope)。

### full · 重做
- **产品锚定 + 场景重做**:产品以**其后的产品原图**为准 100% 保真,第 1 张(源图)仅作此前方向的参考;按用户新要求**重新设计**场景、构图与光线。
- 更接近首次出图,保真锚 = 链根原始产品图(非上轮生成品)。

## 3. 组装序(待 dev 对)

```
[编辑模式块(delta 或 full,自含产品保真)] → [用户编辑指令/新要求] → [modifier 片段(若继承父 job)]
```
- **不拼品类保真块**(§1 冲突;dev compose 编辑分支选编辑块、跳过 CategoryCardRegistry)。
- 编辑指令应必填(与复刻 prompt 选填相反:二次编辑没有指令就没有语义)。

## 4. 开放问题(三方对)

| # | 问题 | 结论 |
|---|---|---|
| Q1 | **多轮迭代产品漂移**(QA D2/TC-05,coordinator #634 点名设计命门) | ✅ **定案:链根双图锚定为 MVP 方案**——喂图第 1 张=**迭代链根的原始产品图**(产品保真锚,零代损耗),第 2 张=被编辑源图(构图/场景锚)。产品每轮都从原始照重建、**漂移不随轮数累积**;单图+强锚定被否(prompt 只能减缓不能消除有损再生成,三代复制的包装小字必糊,等 TC-05 红再改=契约返工)。角色指认沿复刻"序=契约"先例 |
| Q2 | modifiers/ratio/category 继承父 job 还是可改(QA 骨架 Q4) | 产品决策归 PM/dev;提示词层两可(继承→照常拼,不传→退化省略) |
| Q3 | 编辑模式 prompt 必填性 | **必填**(无指令无语义),与复刻选填相反 |
| Q4 | 卡落点 | 第六类「编辑模式卡」`edit-mode-cards/编辑.md`,两档物化块(delta/full),`EditModeRegistry` 沿逐字核对协议+pytest 自动闸 |
