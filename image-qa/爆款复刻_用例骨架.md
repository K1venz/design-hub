# QA 用例骨架 · 爆款图复刻（需求#2）

> 状态：**骨架**（coordinator #540 派、PRD §3.13 已落 commit 1bad38e；HTTP 形态/双角色传参/DB 待三方对，留槽）。
> 竞品参照：`竞品调研-designkit-美图设计室.md` §4。范围=全 A（仅电商商品图/仅手动上传参考图/两档复刻程度）。

## 0. 现行基线（复用优先铁律，QA 锚点）

- 出图引擎**零新增**：listing 链路本就是图生图（`openai_compat._edit()`→`/images/edits` 吃 `reference_images`，`_MAX_IMAGES=3`）。复刻复用 provider/uploads/CostGuard/SSE/ListingHistory。
- uploads 已 owner 隔离（0032/0041 血脉）→ 双角色图都沿用。
- 现状前端=纯占位死按钮（WorkbenchRail replica `enabled:false`），零页面/表单/API。

## 1. PRD §3.13 已定 + 待三方对（QA 断言依赖）

**已定**：
- **双角色输入**：参考图（爆款模板，供结构/风格/构图，**其产品+文案不进产物**，必填 ≥1）vs 产品图（用户商品，**保真主体 verbatim**，必填 ≥1）；合计 ≤3（_MAX_IMAGES 硬顶）。
- **两档保真口径**（命门，coordinator #538 + PRD 表）：
  - **参考风格档**：只学风格/构图/调色，**产品仍是用户的、保真块全生效**。
  - **高度复刻档**：可借模板版式，但**用户产品主体 + 包装文字 verbatim 锁用户上传那张**——「替换文案」=替换参考图营销文案，**绝不改用户产品包装文字**；防被模板原产品带歪、防模板文案糊用户产品（#366+0038 同类防线）。
- overlay_texts **MVP 不进复刻流**（文案诉求走统一要求自由文本=复用 prompt 零新字段）。
- 比例/张数 1/¥0.40/FOOD/Bearer/SSE/TOS/modifiers 收窄全沿用。

**待三方对（留槽，拍定即锁）**：
- **C1 HTTP 形态**（用户拍、dev #541 推荐 (b)）：(a) 扩 `/listing/generate` / (b) 新瘦 `/clone`。**QA 回归成本视角**（中立、决策归用户）：(a) 把单图 n / 套图 plan / 复刻 clone 三模态塞一入口 → 互斥校验矩阵 2 维涨 3 维（n×plan×clone 两两互斥 + upload_ids 语义重载）、边界 400 组合膨胀；(b) 复刻独立 POST /clone、契约自洽，**QA 新增面仅这一端点契约+隔离**，SSE/历史/越权全复用（同一 job 流）。→ 测试简洁度 (b) 占优，与 dev 推荐一致。
- **C2 已定（dev #541）**：显式双字段 `product_upload_ids ≥1` + `reference_upload_ids ≥1`、合计 ≤3、两类全 owns() 404。**喂 /images/edits 图序固定=产品图在前、参考图在后**（image[] 无名序列、语义靠 prompt 锚定「第 1 张=用户产品」）——⚠️ **R2 双角色不混直接依赖此序**：序错/锚定失败 → 模型可能把参考图当主体。QA 无法直接断言序，**RC-03 视觉核（产物主体=用户产品、非模板产品）即此条的兜底验证**。
- **C3 已定**：中文档位 key **参考风格/高度复刻**（prompt 卡 10de548）；非法值→400；**默认档=参考风格**（低风险档先手，coordinator #551/PM #552）。
- **C4 配比·已收敛（prompt #545 提议、待三方对拍）**：**MVP 产品图限 1 张**（`product_upload_ids==1`）+ 参考图 1~2、合计 ≤3 → 指令块纯静态零槽。QA：`product_upload_ids != 1`→400、参考图 0→400。
- **C5 DB 两列·已定都要**（PM #543 + frontend #546，进签字清单、等用户签）：① `listing_job.clone_mode VARCHAR(16) NULL`（复刻档徽标）② `listing_job_input.role VARCHAR(16) NULL`（product\|reference 双角色回显）。均 additive 可空。→ QA：RC-11 加历史档标+角色回显+迁移生效断言。
- **C6 复刻卡·已定**（prompt #542/dev #544）：新**第五类「复刻模式卡」** `CloneModeRegistry{档位→块}`（与 ImageTypeRegistry 平级、复刻**不叠图型卡**）；两档=2 个**纯静态块**（限 1 张产品=零槽）；单一事实源+逐字核对铁律沿用 → **QA 逐字核对清单扩一类（2 块）**。组装序=保真块→复刻档块(含角色指认句)→用户统一要求→modifier。**正式卡已落**（prompt #553，commit 10de548，`image-prompt/clone-mode-cards/复刻.md`，中文档位 key）。⚠️ **预登记张力点**（prompt #553）：通用保真块「严禁堆砌无关物」可能压过高度复刻档要借的模板**道具/版式结构**——QA 视觉核高度复刻档时若发现模板版式被保真块压平，@prompt 调卡措辞（卡协调点、非判 fail）。
- **C7 已定**（PM #552/#555 终拍 + coordinator #551/#557 + dev #556）：复刻路径**统一要求选填**——复刻请求 prompt 缺失/空=**合法**（组装退化为保真块+档位块+modifier）；单图/套图流 prompt 必填 400 **不动**。→ RC-09 锁：复刻 prompt 空=过契约（卡 owns 404，非 400）、单图流 prompt 空=400。

## 2. 验收维度（PRD 6 条 + QA 细化，按优先级）

| # | 维度 | 严重度 | 验收点 |
|---|---|---|---|
| R1 | **两档可区分 + 各自口径** | P0 | 参考风格档：产物产品=用户的、保真，整体风格/构图明显借参考图；高度复刻档：版式借模板但产品主体+文字仍用户的。两档同输入产物**可视觉区分**。 |
| R2 | **命门①竞品泄漏**（最易翻车） | P0 | 参考图的**产品/品牌/文案绝不进产物**（只取风格/结构）；产物主体=用户产品图。核：产物无模板产品的影子、无模板品牌字。 |
| R3 | **命门②产品被带歪 + ③文案错位**（跨两档） | P0 | ② 用户产品主体/包装**不被模板带歪走形**、100% verbatim；③ 模板营销文案**不糊到用户产品**上；高度复刻档「文案位」=用户统一要求给→verbatim 渲染、**未给→留白去字、绝不让模型自编字**（0038 兜底）。多产品（花生+润喉糖×模板）矩阵。prompt #542 点名 QA 视觉核三命门全验。 |
| R4 | **双角色 owner 隔离** | P1 | 参考图 + 产品图两类 upload 都按 owner 隔离；越权引用他人任一类 → **404**（anti-enum 0032/0041）。 |
| R5 | **边界 fail-fast** | P1 | 参考图 0 张→400；产品图 0 张→400；合计 >3→400；复刻档枚举非法→400；overlay_texts 进复刻流→400（MVP 不进）。 |
| R6 | **沿用断言** | P1 | cost reconcile（1 张 ¥0.40）/ SSE+access_token（0011）/ history owner 隔离 / modifiers 收窄全张生效 / ratio。 |
| R7 | **复用零破坏** | P1 | 复刻接入后，现行套图 plan + 单图 n 流既有回归继续全 PASS（复用同 service 不得回归）。 |

## 3. 用例骨架（PRD/三方对锁定后填断言）

| 用例 | 维度 | given → when → then |
|---|---|---|
| RC-01 参考风格档 happy | R1 R3 | 产品图+参考图 + 参考风格档 → 产物=用户产品保真 + 风格借参考、视觉核 |
| RC-02 高度复刻档 happy | R1 R3 | 同输入 + 高度复刻档 → 版式借模板 + 用户产品主体/文字 verbatim、视觉核 |
| RC-03 双角色不混 | R2 | 参考图含明显异产品/文案 → 产物**不含**模板产品、不糊模板文案（核命门） |
| RC-04 两档对比 | R1 | 同输入跑两档 → 产物可视觉区分（风格档保用户构图 vs 复刻档借模板版式） |
| RC-05 保真矩阵 | R3 | 花生/润喉糖 × 两档 × 同一模板 → 逐张核产品保真+无带歪+无糊文案 |
| RC-06 越权·参考图 | R4 | 引用他人参考图 upload id → 404 |
| RC-07 越权·产品图 | R4 | 引用他人产品图 upload id → 404 |
| RC-08 边界·角色数 | R5 | 产品图 0 或 ≠1→400；参考图 0→400（MVP 产品限 1 张） |
| RC-09 边界·超3/枚举/越界字段 | R5 | 合计 4→400；复刻档非法→400；overlay_texts 带入→400；统一要求缺失按 C7 结论 |
| RC-13 文案留白·不自编 | R3 | 高度复刻档 + 用户未给文案 → 产物对应位**留白/去字**、无模型自编中文（0038 兜底视觉核） |
| RC-10 SSE+鉴权 | R6 | access_token 缺/错→拒；事件流完整 |
| RC-11 history owner | R6 C5 | 复刻 job 历史仅本人可见、带复刻档标识（若 C5 落库） |
| RC-12 复用零破坏 | R7 | 套图 plan + 单图 n 流既有回归套件全 PASS |

## 4. 成本闸（沿用两阶段 gate）

- R4/R5/RC-10 等契约/隔离用例：GHOST upload id + 脏输入 → 零出图零成本。
- 真出图：RC-01~05 最小化——1 产品×2 档=2 张起；保真矩阵 2 产品×2 档=4 张一次跑足（¥0.40/张）。跑前报预算。
- prod smoke：部署后 1 单（最小档真出图）+ 落 prod 桶核 + 视觉核，沿默认配置真跑铁律。

## 5. 复用既有资产

- 隔离/anti-enum：`customer_isolation_regression.py`(0041)/`listing_real_boundary.py` C1(0032)。
- 保真×多产品：`listing_multiproduct_regression.py` 扩「双角色×两档」维度。
- 套图回归形态：`taotu_boundary_regression.py`(边界零成本)/`taotu_real_regression.py`(真图+视觉核)/`taotu_prod_smoke.py` 直接套改。
- 逐字核对：若 C6 新「复刻程度卡」，扩 registry 程序化核对。

---
处理记录：
2026-06-10 [QA] 建骨架（coordinator #540 派、PRD §3.13）。已定（双角色/两档保真口径/overlay 不进复刻/复用边界）固化；C1-C6 留三方对。重点 P0：R2 双角色不混 + R3 高度复刻档产品保真不崩（#366+0038 同类命门）。三方对拍 HTTP 形态/传参/DB 后 → 锁 C1-C6 → 细化可跑脚本（形态依 C1 决定扩套件 or 新端点）。
