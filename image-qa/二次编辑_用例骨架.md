# QA 用例骨架 · listing 二次编辑（ISSUE-0040，基于结果迭代再生成）

> 状态：**refine 锁定中**（PM §3.12.13 摘要 #642/#643 + 用户拍 3 scope #644；端点 shape/喂图上限/Q-β 待三方对）。
> schema 三列已签上线（parent_job_id/source_image_key/edit_mode，随套图迁移）→ **本轮零新迁移预期**。

## 0. 功能（已定）

用户拿**自己某次成功出图**（单图/套图张/复刻产物皆可，Q-δ）+ 新 prompt + edit_mode → 再过一次 `/images/edits` → 出 **1 张**（Q-ε）新图、独立新 job + parent_job_id、落 generate 桶、复用 provider/cost guard/SSE/history **零新出图链**。可迭代多轮（深度/分叉无硬上限 YAGNI）。
- **端点（Q-α lean，dev 定）**：新瘦路由 `POST /listing/edit`（沿复刻 (b) 先例）。
- **入口可寻址字段（#638/#639 锁）**：走**读模型**（ListingImageOut 加可寻址 handle）、**SSE 契约冻结**（图的身份只一份）；前端 task_completed 后补拉 job 详情取 handle。

## 1. 已定契约（QA 断言依据，#642/#643/#644）

- **edit_mode ∈ {delta, full}**、delta=默认。
  - **delta（微调）**：锁产品本体+品牌文字+**上一版构图基底**、只 apply 增量；**ratio/category 继承父 job**（换比例=重构图≠编辑）；prompt/modifiers 叠新。
  - **full（重做）**：上一版作场景起点 + 全新重绘；**ratio/category 可改**；产品+文字仍锁死。
- **D2 链根锚（硬约束，#635/#637/#642）**：保真锚 = **迭代链根的原始上传产品图**（uploads 桶 role=product，parent 链走到 NULL=根）、**绝不取上一轮产出**（失真不可逆、锚上一轮=漂移逐轮叠加）。**两档都带锚**。喂图 = 源图(被编辑基底)1 + 链根锚 1..3 = **2..4 张**（复用 /clone 双喂机制、喂序即角色、块内角色识别句）。clone 产物的链根锚 = clone 时的产品上传图（通路同构）。
- **Q-γ 编辑不引图型卡、不支持 overlay_texts**（改图上字=对卖点图 full 重做）；源图是套图某图型张时，图型徽标**继承父张 image_type 仅作展示、不进组装**。
- **owner 隔离 P0**：源图 job + 链根 job **双核 user_id**；非本人/不存在/畸形/**失败张作源** → **404 anti-enum**（0032，不区分不存在与存在但失败/非本人；防 0039）。
- **成本**：每次 edit = 1 次真实 edits（n=1）→ 按次单计入 cost_ledger + 预算闸；UI 展示**单次 + 迭代链累计**。
- **回显**：SummaryOut 带 edit_mode（null=原生单 / ✎微调 / 重做 轻徽标）；DetailOut 带 parent_job_id + edit_mode + 源图回显（source_image_key 签 url「改自这张」、只读不可换）。
- **async+SSE 同 0011**、失败 fail-fast；quality MVP 默认档（Q-ζ，不压字=无 high 需求）。

## 2. 验收维度（按优先级）

| # | 维度 | 严重度 | 验收点 |
|---|---|---|---|
| D1 | **迭代链 owner 隔离** | P0 安全 | 源图 job + 链根 job 双核本人；他人/不存在/畸形/失败张作源 → **404 anti-enum**（0032，返回不可区分）。 |
| D2 | **保真不崩 + 链根锚抗累积失真** | P0 功能 | 产品本体+包装文字 verbatim；**多轮迭代漂移不随轮数叠加**（每轮锚链根原图、非上一轮）。delta 锁构图基底、full 放构图但产品+文字仍锚死。两档分验。**⚠️ 验收=视觉核标准非像素冻结**（PM #654③）：/images/edits 整图重生成，未点名区域的**生成性细微差异不计 fail**；判 fail 的是『显著改动外溢』+『漂移随轮数累积』，与复刻视觉核同标准（防验收阶段 QA/dev 打架）。 |
| D3 | **二次 prompt 生效** | P1 功能 | 新诉求体现（例「背景换纯白」→ 背景白）**且不破坏 D2**。 |
| D4 | **成本按次计** | P1 成本 | 每次 edit 各计一次、ledger 不漏不重；迭代链累计展示正确。 |
| D5 | **源选择 + 入口正确** | P1 功能 | edit 用的源 = handle 指向的那张成功结果图；parent_job_id 指源 job；源图只读不可换。 |
| D6 | **async + SSE 一致** | P1 一致 | 同 0011 access_token；事件流完整；SSE 契约冻结（无新增字段）。 |
| D7 | **失败 fail-fast** | P2 健壮 | provider 错 → job 失败、不假图、计费可追溯。 |
| D8 | **边界 + Q-γ/δ/ε** | P2 边界 | 失败张/他人源→404；overlay_texts 传入=无效（Q-γ）；不引图型卡；一次出 1 张；edit_mode 非法→400。 |

## 3. 用例（断言随三方对锁定）

| 用例 | 维度 | given → when → then |
|---|---|---|
| TE-01 delta happy | D2 D5 D6 | 自己成功图 + delta prompt → 1 张、SSE 全、产品+文字+**构图基底**保真、只 delta 变、parent 指源、落 generate 桶、cost 单计 |
| TE-02 full happy | D2 | full prompt → 产品+文字锁死、构图/背景重做、ratio/category 可改 |
| TE-03 **链根锚·多轮 delta** | D2 | 同链连做 **3 轮 delta** → 逐轮视觉核：产品本体/文字 verbatim、**漂移不随轮数累积**、无显著改动外溢（视觉核标准非像素冻结、生成性细微差异不计 fail，PM #654③） |
| TE-04 **链根锚·full 锁产品** | D2 | full 重做 → 构图变 but 产品+文字仍 verbatim（链根锚压住） |
| TE-05 越权·他人源 | D1 | B 编辑 A 的结果 handle → 404 |
| TE-06 越权·不存在/畸形/失败张 | D1 D8 | 不存在/畸形/**失败张**作源 → 404（与他人源不可区分） |
| TE-07 源范围（Q-δ） | D5 | 套图某图型张 / 复刻产物 / 单图 各作源 → 都可编、各自链根锚生效 |
| TE-08 delta 继承 vs full 可改 | D8 | delta 改 ratio → 继承父（拒/忽略按 shape）；full 改 ratio → 生效 |
| TE-09 Q-γ 不引图型卡/overlay·extra=forbid | D8 | 源=套图卖点张 → 产物不带图型卡 styling；**overlay_texts/未知字段传入 → 422**（E-⑥ dev #648：/listing/edit 首个 extra=forbid 端点、兑现 0044 方向） |
| TE-10 二次 prompt 生效 | D3 | 「背景换纯白」→ 背景白 且 产品保真 |
| TE-11 成本按次+累计 | D4 | N 次 edit → 各计一次入 ledger（**必验**）；迭代链累计展示=服务端聚合字段则验，MVP 降档仅显单次则不 fail（链累计 fast-follow，PM #654①——E-④ dev 方案定聚合 or 降档） |
| TE-12 SSE 鉴权 | D6 | access_token 缺/错 → 拒（0011） |
| TE-13 失败 fail-fast | D7 | 注入 provider 失败 → job 失败、不假图、计费可追溯 |
| TE-14 回显 | D5 | SummaryOut edit_mode 徽标（原生 null/✎微调/重做）、DetailOut parent_job_id+edit_mode+源图回显 |
| TE-15 一次 1 张（Q-ε） | D8 | edit → images 恰 1 张 |
| TE-16 边界脏输入 | D8 | **edit_mode 非法→400**（registry，dev #657）；缺 source_image_key→422；**prompt 空→422**（min_length=1）；**delta 显式传 ratio→400**（拒，full 可传）；未知字段/overlay/category→422（extra=forbid） |

## 4. 待三方对开放点（剩，QA 断言依赖）

**已决（coordinator #648 / prompt #645）**：
- ✅ **E-β 两档都不带父 prompt 进组装**（prompt #645：视觉锚＞文本二传、防与本轮指令打架、宪章 §2.5）→ D3 锁「父诉求不残留、本轮指令纯生效」。
- ✅ **E-ε edit prompt 两档必填，空→422**（coordinator #653 收敛：pydantic `min_length=1`、与 extra=forbid 同层 422 口径，dev 落字按 min_length=1）→ TE-16 锁 422 单值。
- ✅ **E-ζ overlay/未知字段 = extra=forbid→422**（dev #648：/listing/edit 新端点直接 extra=forbid、兑现 0044 方向）→ TE-09 锁 422（非静默忽略）。
- ✅ **编辑卡 = 第六类 EditModeRegistry{delta,full}**（prompt d9d4f21，两档纯静态零槽、不注入品类块/图型卡/父 prompt、自含保真+链根锚块内落点）→ QA 逐字核对扩到 **9 物化块**（pytest 自动闸顺带覆盖，dev 扩）。

**已定（dev #657 b37e109，三方对终局输入）**：
- ✅ **E-α shape**：`POST /listing/edit`，EditRequest = `source_image_key` + `edit_mode`(默认delta、非法→**400**) + `prompt`(min_length=1→**422**) + `modifiers`(两档叠新 {**parent,**req}) + `ratio`(delta 显式传→**400** 拒、full None 继承/显式覆盖)；**extra=forbid**；**不收 category**（R2 编辑无品类块、收=死参数）。
- ✅ **E-δ handle = `source_image_key`**（sha256(bytes)[:16] 不透明 handle、构造性防枚举、与已签列零翻译）。**反解谓词** = `key ∧ job.user_id=本人 ∧ status=成功 ORDER BY created_at DESC LIMIT 1`——一条查询同时完成 D1 第一核 / 失败张 404（Q-δ）/ 多行收敛 / 无行 404，全 anti-enum 同文案。→ **TE-05/06 越权构造** = 随机 sha（不存在）/ 他人真实 key（非本人）/ 失败张 key（status≠成功）→ 都 404。
- ✅ **E-γ 喂图** = [源图1]+[链根锚1..3] = 2..4 张（协议 16 内）；链根锚谓词 `role='product' OR role IS NULL` 统覆 clone 根(product 1) + 套图/generate 根(NULL 全产品 1..3)；4 张 dev 联调首单冒烟实证（回退=锚取根首张仍零失真不破 D2）。
- ✅ **chain_cost 服务端聚合**（frontend④）：读时沿链一次聚合 O(深度)、零新列；语义=根计「源张」单张 cost（根是 5 张套图算 ¥0.40 不算 ¥2.0）→ TE-11 据此。
- ✅ **回显字段**：ListingImageOut 补 `image_key`（=路由段 handle）；DetailOut 补 parent_job_id/edit_mode/source_image_url/source_image_type；SummaryOut 补 edit_mode。**零新迁移/零新表/SSE 冻结**。

**待 PM 确认**：R2（PRD「full 可改 category」→「不适用」）+ R5（chain_cost「根算源张单张」口径）。

## 5. 成本闸 + 复用

- 边界/owner（D1/D8 大部分）：handle 越权/脏输入 → 零出图零成本（404/400 在出图前）。
- 真出图最小化：TE-01/02（delta+full 各 1 张 ¥0.40）；**TE-03 多轮 3 轮 delta = ¥1.20**（链根锚抗失真核心）；TE-07 源范围借已有产物作源。跑前报预算。
- prod smoke：部署后 1 单 edit（delta 最小）+ 落 prod 桶 + 链根锚视觉核 + 回显，沿默认配置真跑铁律。
- 复用：owner 404 = `customer_isolation_regression.py`(0041)/`clone` 越权；双喂机制 = clone 脚本；SSE/历史 = `listing_history_e2e.py`；成本两阶段 gate；逐字核对若新增 edit 模式卡则扩 registry。

---
处理记录：
2026-06-09 [QA] 建骨架（coordinator #399 派、PRD 前置）+ 折 delta/full + crosswalk。
2026-06-11 [QA] **按 PM §3.12.13 摘要（#642/#643）+ 用户拍 3 scope（#644）refine**：D2 升级为「链根锚抗累积失真」硬约束（每轮锚链根原图、漂移不叠加，TE-03/04 两档分验）；折入 Q-γ 不引图型卡/不 overlay、Q-δ 所有成功出图可编+失败张 404、Q-ε 1 张、端点瘦路由 /listing/edit、入口走读模型 SSE 冻结、回显 SummaryOut/DetailOut。剩 E-α~E-ζ 待三方对（端点 shape/Q-β 父 prompt/喂图上限/handle 字段/prompt 必填/overlay 处置）→ 拍定即出可跑脚本（复用 clone 双喂+多轮失真视觉核）。
