# listing 一键出图 后端测试报告（ISSUE-0023）

- 日期：2026-06-04
- 验证人：QA
- 被测：`POST /listing/generate`(multipart, Bearer) + `GET /listing/{job_id}/events`(SSE)，main b8ebfc4→ff4593c
- 真相源：spec `2026-06-04-listing-image-generation-design.md`、ISSUE-0023 用例
- 手段：Layer1 纯单元（直接 import，无 DB）+ Layer2 HTTP/SSE（httpx ASGITransport in-process + Mock provider，无 DB，token 直 mint）。**全程 Mock，零真实出图成本。**
- 脚本：`image-qa/listing_test_unit.py`、`image-qa/listing_test_http.py`
- 结论：**Layer1 22/22 全过；Layer2 12/16，4 个失败=同一真实 bug（边界校验未 fail-fast，已开 ISSUE-0024）。** 业务逻辑/SSE/成本守门均正确。

## 一、结果总览

| 层 | 通过 | 说明 |
|---|---|---|
| Layer1 单元 | 22/22 | prompt 组装、ratio→size、未知下拉 fail-fast、service 出图/成本预扣回正/失败回滚、service 层 n·图数边界 |
| Layer2 HTTP/SSE | 12/16 | 4 失败=边界校验未 fail-fast（详见缺陷①）|

## 二、逐用例（ISSUE-0023 §需 QA 覆盖）

| # | 用例 | 结果 | 证据 |
|---|---|---|---|
| 1 | 1/2/3 图 + 合法 modifiers → job_id | ✅ | HTTP 200 + `{job_id}` ×3 |
| 2 | 0 / >3 图 → 4xx | ✅(码不符) | >3→**409**「图片数量需为 1..3」；0→FastAPI **422**。**spec 要 400**（缺陷②）|
| 3 | n 越界(0/8/负) → 4xx | ❌ | HTTP **200+job_id**（应 4xx；service 层抛 DomainError 但在异步命令里 → task_failed）。缺陷① |
| 4 | modifiers 非法JSON/非对象 → 4xx | ✅(码部分不符) | 非法JSON→**400**✅；非对象→**409**（spec 要 400）|
| 5 | 未知下拉值 → 4xx | ❌ | HTTP **200+job_id**（应 4xx）。缺陷① |
| 6 | 不支持 ratio → 4xx | ❌ | HTTP **200+job_id**（应 4xx）。缺陷① |
| 7 | 无 Bearer→401；SSE 无 access_token→401 | ✅ | 均 401 |
| 8 | SSE 全序列逐张到达 | ✅ | `task_started→model_called→image_generated×3→task_completed` |
| 9 | 出图失败→task_failed（不静默吞）| ✅ | fail=True provider → `[task_started, task_failed]`，异常上抛不吞 |
| 10 | 晚订阅不丢事件（回放）| ✅ | 延迟 0.6s 订阅仍收齐全序列（InMemoryEventBus 回放）|
| 11 | prompt 含自由文本 + 各片段（顺序/分隔）| ✅ | `用户文本。片段A；片段B；片段C`，用户文本在首 |
| 12 | ratio→size 映射 | ✅(有超集) | 1:1→1024²、3:4/9:16→1024×1536、16:9→1536×1024；**额外接受 4:3**（缺陷③）|
| 13 | 成本预扣→回正；失败回滚 | ✅ | n=3 net=3.57=实际；provider 失败后 net=0（额度不漏）|
| 14 | 真实 gpt-image-2 多图 edit | ⏸ 待受控环境 | 需 GPT_IMAGE_* key + MySQL + 花钱（plan §5.3），与运维/开发协调，本轮未跑 |

补充：空 prompt 在 HTTP 边界也返回 200+job_id（应 4xx），同缺陷①。

## 三、缺陷

### ① [主] 边界校验未 fail-fast：n/ratio/未知下拉/空prompt 返 200+job_id 而非 4xx（→ ISSUE-0024，P2）
- spec §4.1(line 82)「校验在边界完成（fail-fast）：images 数量、n 范围、ratio 合法、modifiers 可解析」、§7(line 159-160)「边界校验失败（图数/n/ratio/modifiers/未知下拉值）→ 4xx，不降级」。
- 实测：路由 `listing.py` 只同步校验「图数 1..3」与「modifiers 是 JSON 对象」；**n 范围、ratio 合法、未知下拉值、空 prompt 都在 `ListingGenerationService.generate()`（异步命令内）才校验** → HTTP 先返回 200+job_id，错误经 SSE `task_failed` 暴露。
- 影响：调用方拿到 job_id 以为成功、需订阅 SSE 才知失败；与"边界 fail-fast 4xx"契约不符。错误未静默（仍 task_failed），故 P2 非 P0/P1。

### ② [次] 边界 4xx 错误码不符 spec：DomainError→409 / 0图→422，spec 要 400（→ ISSUE-0024，P3）
- 图数越界、非对象 modifiers 走 `DomainError`→边界映射 **409**；0 图 FastAPI **422**。spec/ISSUE-0023 均写 **400**。
- 建议：边界输入校验错误统一 400（与 ValueError 一致），DomainError(409) 留给真实领域冲突。

### ③ [轻] sizing 接受 4:3（超出确认集）（→ ISSUE-0024 备注，P3）
- `_RATIO_TO_SIZE` 含 `4:3→1536×1024`，超出 ISSUE-0021 确认的 1:1/3:4/9:16/16:9。无害但属未确认取值，建议与 PM 对齐保留或删除。

## 四、未覆盖 / 依赖
- 用例 14 真实多图 edit（image[] 多字段是否被中转站支持、不支持时退化为并发逐图）→ **待受控环境**，与 Dev/运维配合（plan §5.3）。
- 验收阈值（成功率/时延/成本红线）依赖 PM ISSUE-0021；话术正式文案 ISSUE-0022。本轮验「契约/逻辑正确性」，未验"质量阈值"。
