# 去 Redis（单进程内存）全流程回归报告

> 本文保留 2026-06-03 的历史执行证据。当前复跑必须通过 `qa_auth.py` 使用运行时提供的已验证账号登录；常规 smoke 不再自动注册或发送验证邮件。

- 日期：2026-06-03
- 验证人：QA
- 背景：开发完成「异步出图+SSE 去 Redis → 单进程 `InProcessTaskQueue` + `InMemoryEventBus`」重构（commit fc41318/f548306）
- 结论：**先验准确性（去 Redis 声明逐条属实）→ 再跑全流程 35/35 全绿**。异步出图/SSE 单进程化端到端跑通，**全程零 Redis、零 worker、零新真实出图成本**。

## 一、准确性核查（开发声明逐条对账）

| 开发声明 | 核查 | 结果 |
|---|---|---|
| ruff+mypy 全绿(173) | `ruff check` All passed；`mypy` Success: 173 files | ✅ |
| 依赖无 arq/redis/hiredis | `grep` pyproject/uv.lock 无命中 | ✅ |
| 源码无 Redis/arq | 无 `import redis/arq`、`RedisSettings/create_pool/RedisEventBus/ArqTaskQueue` | ✅ |
| 删 worker.py/redis_bus.py/arq_queue.py/payload.py | 4 文件均已删 | ✅ |
| asgi 换单进程内存适配器 | lifespan 用 `InMemoryEventBus`+`InProcessTaskQueue(runner)`，无 Redis pool | ✅ |
| 契约 /generate/async + /events 不变 | openapi 路径仍在 | ✅ |
| Settings.redis_url 删除 | settings.py 已无该字段 | ✅ |
| 起服务只需 uvicorn | 仅起 1 个 uvicorn(无 worker/无 REDIS_URL) 跑通全异步 | ✅ |

> 唯一残留（doc nit，非功能）：`asgi.py` 模块 docstring（1–5 行）仍写「MySQL + Redis + arq」「需 DB_URL / REDIS_URL」。
> 不影响运行，建议开发顺手清理（归入 ISSUE-0016 跨角色文档清理）。

## 二、环境（去 Redis 后简化）

- 起服务：**仅一个 uvicorn**
  `DB_URL=mysql+aiomysql://root:***@127.0.0.1:3306/image_gen SEED_ADMIN_EMAIL=… SEED_ADMIN_PASSWORD=… uv run uvicorn design_hub.interface.api.asgi:app --port 8000`
  —— **无 arq worker、无 REDIS_URL**。异步出图在 API 进程内经 `asyncio.create_task` 跑 `GenerationTaskRunner.run`。
- DB：`alembic upgrade head` 新增 `app_user` 表（迁移 `4a2a261611d9`，自建认证 ISSUE-0015）。
- 当前复跑认证：`QA_USER_EMAIL`/`QA_USER_PASSWORD` 与 `ADMIN_EMAIL`/`ADMIN_PASSWORD` 在运行时注入，统一经 `/auth/login`；注册邮件链路由独立交互验收脚本覆盖。
- SSE 鉴权：`?access_token=<JWT>`（ISSUE-0011，原生 EventSource 不能带头）。
- 成本：异步/同步出图全路由 Mock（family_3→seedream，0 元）；**导出复用上轮真实 gpt-image 出图 id=3（file://）**，无新真实调用。

## 三、全流程结果（35/35 通过）

| 链路 | 结果 | 关键证据 |
|---|---|---|
| 1 认证（自建邮箱密码） | ✅ | 历史执行曾创建设计师；当前复跑为已验证设计师/管理者登录；/me 双角色；无 token 401；错密码 401 |
| 2 角色矩阵（含新 /admin/users） | ✅ | 管理者 /admin/models·/dashboard/cost·/admin/users 均 200；设计师均 403 |
| 3 工作台 CRUD | ✅ | 客户#3→项目#3(需求录入)→需求单 8 字段→素材#3 全 200 |
| 4 同步出图(mock) | ✅ | seedream-5 出图，job 挂 project_id=3+round_no=1 落库 |
| **5 异步+SSE（去 Redis 核心回归）** | ✅ | **5a** 入队 200；**5b** 即时订阅收齐 `task_started→model_called→image_generated→task_completed`；**5c 晚订阅 1.5s 仍全序列回放（内存总线不丢 task_started）**；**5d** 无 token SSE→401 |
| 6 选稿 | ✅ | 列候选图→评分4→保留→可用率 {1/1,1.0} |
| 6b 项目级枚举(新 project_catalog) | ✅ | `/projects/{id}/jobs`=1、`/projects/{id}/images`=1 |
| 7 改稿单 409/force | ✅ | 录入→设计中→审稿；未完成条目转已交付 409；?force=true→200 |
| 8 导出（复用真实图） | ✅ | image_ids=[3] 导出 jpg/png/pdf+zip，命名归档 `P0001_…_20260603_061539.zip` |
| 9 仪表盘 5 维 | ✅ | overview/model/project/designer/tier 全 200 |
| 10 监控 /metrics | ✅ | 200 + `design_hub_generations_total{mode="text2img",model="seedream-5"} 3.0` |

## 四、单进程异步落库实锤（DB 核对）

```
generation_job: 91f9…/d5bd…(async, project_id=NULL) + 28c4…(sync, project_id=3) 均 status=done, used_model=seedream-5
→ 证明 worker 删除后，异步任务仍在 API 进程内真正执行 pipeline 并写库（不只是发事件）。
app_user: (1) qa-admin@test.com 管理者[seed]  (2) qa-designer@test.com 设计师[register]
```

## 五、结论

- **去 Redis 重构准确无误、全流程回归 100% 通过**。异步出图+SSE 单进程化后行为与原 Redis 版一致（含晚订阅回放），契约/端口不变。
- 我的回归脚本已更新为单进程版（`e2e_driver.py`，无 Redis/worker），对应 ISSUE-0016 的 QA 清理项已落实。
- **未覆盖（待授权）**：真实 gpt-image 出图本轮全程未触发（去 Redis 与真实 Provider 无关，上轮已验文生图可用）。
  ISSUE-0007 半②（真实 edit 出真图）仍待用户授权预算后复验。需要的话我用真实库补跑 1 次真实出图终验。
