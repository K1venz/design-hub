# ISSUE-0053「配方展示+一键复用」独立验收报告

- **验收日期**：2026-07-07
- **验收人**：QA（独立复验，非开发自测）
- **被测提交**：b86d4e0（后端）+ 7443b3d / 398639e / bd09745 / d907694（前端），均在 main 最新
- **环境**：本机 mock 模式（REAL_GPT_IMAGE=false，sqlite DB，mock图像，出图零成本）
- **结论：5 条验收全绿 + 3 条边界全绿，0 FAIL**

---

## 一、被测提交确认

```
3db5846 chore(issue): ISSUE-0053「查看详情」弹卡视觉/动画皮追记（d907694）
d907694 feat(web): 0053「查看详情」弹卡上参考件卡形 + staggerChildren 动画
f639a79 chore(issue): ISSUE-0053 落点B细化增量追记（bd09745）
bd09745 feat(web): 0053 落点 B 细化——showcase 卡双按钮 +「查看详情」卡片弹层
398639e feat(web): 0053 落点 B——首页成果区配方展示 +「做同款」登录墙携配方预填
7443b3d feat(web): 0053 落点 A——出图历史/结果卡「查看配方」+ 一键复用配置预填
b86d4e0 feat(showcase): 落点B配方展示——ShowcaseEntry扩recipe+prod五单只读回填
```

所有目标提交均在 main 历史中确认。

---

## 二、验收 5 条

### 条件 1：落点 A（历史详情/工作台结果卡「查看配方」+ 「复用配置出图」）

**结论：PASS**

操作步骤：
- 注册账号 → /set → 上传花生白底图 → 配比：白底1/场景2/卖点2，比例：9:16→手动改为3:4，平台：抖音电商，风格：高山田园清新风… → 点生成

验证点：
- **工作台结果卡**：右侧标题栏出现「查看配方」按钮 ✓
- **历史详情页**（/history/30fddbb7de63452e8dcb88bffbc950f8）：标题行有「查看配方」按钮 ✓
- **弹层内容精确一致**（与提交请求体逐项比对）：
  - 图型配比：白底图×1 场景图×2 卖点图×2 共5张 ✓
  - 比例：3:4（改后值，非原9:16） ✓
  - 地区：中国 / 语言：中文 / 平台：抖音电商 ✓
  - 风格描述：「高山田园清新风，远处青山与蓝天，竹匾晾晒花生的自然场景，通透明亮的阳光，竖版构图上方留白」 ✓
- 「复用配置出图」按钮 → /set：
  - 上传区为空（无已上传图片） ✓
  - 生成按钮 disabled（无上传触发不可提交） ✓
  - 图型配比/比例/风格描述/平台全预填 ✓

---

### 条件 2：落点 B（showcase 双按钮 + 查看详情弹层 + 做同款登录墙回跳）

**结论：PASS**

验证点：
- 首页成果区进入视口后拉取 /showcase，13 条真实数据正常渲染 ✓
- 每张卡上：「查看详情」按钮 + 「做同款」按钮（双按钮） ✓
- **登出态点「查看详情」**：弹层直开，不跳 /login ✓
- **弹层全项齐备**（与 /set 生成界面配置项一一对应）：
  - 品类：食品（FOOD→中文展示名） ✓
  - 图型配比：白底图×1 场景图×2 卖点图×2 共5张 ✓
  - 比例：1:1 ✓
  - 平台：淘宝天猫1688 ✓
  - 地区·语言：中国·中文 ✓
  - 风格描述：「暖调原木餐桌与米色麻布衬底，散落几粒带壳花生点缀，柔和自然晨光，温馨休闲零食氛围，画面干净高级」 ✓
- 弹层内「做同款」CTA 存在 ✓
- **弹层内做同款（登出态）→ /login**：
  - router state 携带 `from: {pathname: '/set'}` 和完整 prefill ✓
  - prefill 确认值（花生·高山晾晒场景卡，另一次测试）：ratio="9:16"，platform="抖音电商"，styling="高山田园..." ✓
- **登录后回跳 /set**，预填完整应用 ✓

---

### 条件 3：泄漏面（showcase 响应及 UI 弹层无内部卡 prompt）

**结论：PASS**

API 响应检查（`curl http://127.0.0.1:8002/showcase`）：
- 响应总长 8372 chars，13 条 entry
- 每条 recipe 字段仅含：`category / ratio / plan / styling / modifiers` ✓
- 无额外字段（prompt、card、system_prompt 等）✓
- 全文扫描「pure white background / photorealistic / ultra-high / studio / 保真 / 宪章 / 卡体系」：**无命中** ✓
- 「CATEGORY」命中 = 字段名 `category` 的值 `"FOOD"`，为用户层品类标识，非内部卡措辞 ✓
- UI 弹层展示的 styling 文本为用户风格描述（中文场景描写），非英文产品摄影指令 ✓

---

### 条件 4：预填可改（改后按改后值提交）

**结论：PASS**

操作：从 showcase「做同款」→ /set 预填 ratio=9:16 → 手动改为 3:4 → 上传图片 → 点生成

抓取 POST /api/listing/generate 请求体：
```json
{
  "ratio": "3:4",
  "prompt": "高山田园清新风，...",
  "plan": {"白底":1,"场景":2,"卖点":2},
  "modifiers": {"region":"中国","language":"中文","platform":"抖音电商"}
}
```

ratio 为改后值 3:4，非原预填 9:16 ✓。其余字段（prompt/plan/modifiers）均按改后当前值提交。

---

### 条件 5：零回归（老路径各过一遍）

**结论：PASS**

| 路径 | 结果 | Console 错误 |
|---|---|---|
| 首页 showcase 懒加载（进视口才拉 /showcase） | 正常，placeholder → 真实卡 ✓ | 0 |
| /history 历史列表 | 正常渲染，两条记录 ✓ | 0 |
| /history/:id 详情 | 正常渲染，回看各图 ✓ | 0 |
| /set 套图出图 | 正常生成5张 ✓ | 5（mock://协议，见注）|
| /set 单图出图 | 正常生成1张 ✓ | 1（同上） |
| /chat 帮我设计 | 正常载入，0051会话侧栏在 ✓ | 0 |

**注**：/set 出图后 console 报 `net::ERR_UNKNOWN_URL_SCHEME @ mock://gpt-image-2/N.png`，为 MockModelProvider 返回 `mock://` scheme URL 导致浏览器无法加载图片，**是 mock 模式固有行为**（历史版本已存在），与本次特性无关，非回归。

---

## 三、加测边界（独立价值）

### 边界 a：预填后 F5 刷新 /set

**实际行为：完全干净重置（非残留一半）**

分析：WorkbenchPage.tsx 的 prefill useEffect 在应用预填后立即执行
`navigate(location.pathname, { replace: true, state: null })` 清空 history state；
F5 后 zustand store 重置为默认值，history state 已为 null，故所有字段回默认。

观测：F5 后 /set 显示默认值（淘宝天猫1688 / 1:1 / 空风格描述），无任何预填残留。

**判定：行为一致，干净重置，不属于「残留一半」bug。**

---

### 边界 b：连续预填覆盖完整性

**结论：PASS（无串值）**

操作：
1. 历史复用（抖音电商 · 3:4 · 花生高山田园配方）→ 应用到 /set
2. 返回首页 → showcase「做同款」第13张（润喉糖·蜂蜜暖调卖点 · 京东 · 3:4）

/set 结果：
- 电商平台：京东（非抖音电商） ✓
- 风格描述：「清爽洁净的白色大理石台面，蓝黄品牌色呼应的背景...」（非花生高山田园） ✓

前者风格描述未串入后者，覆盖完整。

---

### 边界 c：单图/复刻/编辑单——「复用配置出图」仅套图单出现

**结论：PASS**

- **单图单**：生成1张后，「查看配方」弹层底部显示「复刻 / 编辑 / 单图单暂不支持一键复用，可参考上方配方手动新建。」，无「复用配置出图」按钮 ✓
- **CloneWorkbenchPage.tsx / EditWorkbenchPage.tsx**：grep 确认两个页面均无 RecipeDrawer 组件引入 ✓
- **HistoryDetailPage.tsx**：引入 RecipeDrawer，对复刻/编辑/单图单 job（reusable=false）渲染「不支持」文本而非复用按钮（代码逻辑 + 单图单实测验证） ✓

---

## 四、覆盖矩阵

| 条目 | 状态 | 证据 |
|---|---|---|
| 条件1·工作台结果卡「查看配方」 | PASS | playwright snapshot，按钮存在 |
| 条件1·历史详情「查看配方」 | PASS | playwright snapshot，按钮存在 |
| 条件1·弹层内容精确一致 | PASS | snapshot与网络请求体逐字比对 |
| 条件1·「复用配置出图」→/set 上传区空+按钮disabled | PASS | snapshot确认 |
| 条件2·双按钮存在 | PASS | 13张卡均有「查看详情」「做同款」|
| 条件2·登出态「查看详情」不跳登录 | PASS | 直接打开弹层，URL不变 |
| 条件2·弹层全项齐备（6项） | PASS | snapshot逐项确认 |
| 条件2·做同款+登录墙+回跳预填 | PASS | history.state JSON逐字检查 |
| 条件3·API无内部卡prompt | PASS | curl响应全文扫描无命中 |
| 条件3·UI弹层无内部卡prompt | PASS | snapshot content检查 |
| 条件4·改值后按改后值提交 | PASS | 网络请求体 ratio="3:4" |
| 条件5·历史/chat/set/showcase零回归 | PASS | 各页正常，console记录见上 |
| 边界a·F5干净重置 | 干净重置（PASS） | 全部字段回默认 |
| 边界b·连续预填无串值 | PASS | 第二次预填完整覆盖第一次 |
| 边界c·单图单无复用按钮 | PASS | 弹层显示「不支持」文本 |

**总计：15/15 通过，0 失败**

---

## 五、遗留已知限制（不属于 bug）

1. **overlay_texts 不进配方**：符合 spec §一裁决 (a)，卖点文案未持久化，复用他人文案不成立，属设计决策。
2. **mock:// console 报错**：MockModelProvider 固有，与本次特性无关。
3. **showcase 图片 404**：TOS bucket 在 mock 环境下图片 URL 签名有效但无真实文件，img 加载失败（但不影响功能，占位 bg-wb-surface-3 兜底）。
